{
  "goal_id": "aos-p0e-ams-parent-link",
  "end_state": "In the single file ap-web/src/lib/ams.ts, make exactly this change:\n\nPreserve the server's authoritative parent link: parent_execution_id must fall back to the top-level field when the task blob omits it, instead of overwriting it with null.\n\nChange nothing else, in this or any other file.\n\nHOW TO WORK:\n  - Read the file with read_file before changing it. Never edit a file you have not successfully read, and never invent its contents.\n  - Make the change with a write_file call containing the complete new file contents.\n  - Then run BOTH gate commands verbatim as run_shell calls:\n      npm --prefix ap-web run type-check\n      npm --prefix ap-web test\n    Run them exactly as written. A scoped variant (adding a filename) does not satisfy the gate.\n  - Set done=true once the edit is written and both commands have been run.\n  - Change nothing outside the single file named above.",
  "evidence_criteria": {
    "syntax_command": "npm --prefix ap-web run type-check",
    "test_command": "npm --prefix ap-web test",
    "evidence_artifact_required": false,
    "required_files": [
      "ap-web/src/lib/ams.ts"
    ]
  },
  "inspection_criteria": [
    "ams.ts uses a nullish fallback to the top-level parent_execution_id.",
    "Only ap-web/src/lib/ams.ts was modified.",
    "No test was deleted, renamed away, or marked skip/todo/only to make the suite pass.",
    "The change is small, local, and reviewable in a diff."
  ],
  "blocked_conditions": [
    "The vitest suite cannot be made to pass without deleting or skipping a test that is asserting correct behaviour.",
    "A required change would need edits outside ap-web/src.",
    "The TypeScript build fails for a reason unrelated to these changes.",
    "npm install / dependency resolution fails in the workspace."
  ],
  "budget": {
    "max_total_tokens": 250000,
    "max_wallclock_s": 1800,
    "max_usd": 8.0
  }
}