{
  "goal_id": "aos-exec-preflight-v2",
  "end_state": "Prove the AOS goal bridge can execute shell commands and write files inside the allowlisted workspace. Do exactly three things, each as a run_shell or write_file tool call, and emit each subgoal's verification_command as a run_shell call in the same response as the work it verifies.\n1. Run `git rev-parse --abbrev-ref HEAD` and observe the branch name.\n2. Write that branch name as the single line of a new file EXEC_PREFLIGHT.txt at the workspace root.\n3. Run `test -s EXEC_PREFLIGHT.txt` and observe it exit 0.\nScope: create only EXEC_PREFLIGHT.txt. You are not required to prove the absence of other changes; the gate below is the whole acceptance test.",
  "evidence_criteria": {
    "required_files": [
      "EXEC_PREFLIGHT.txt"
    ],
    "syntax_command": "test -s EXEC_PREFLIGHT.txt",
    "evidence_artifact_required": false
  },
  "inspection_criteria": [
    "EXEC_PREFLIGHT.txt exists at the workspace root and is non-empty.",
    "Its single line is the current git branch name."
  ],
  "blocked_conditions": [
    "The run has no ability to execute commands.",
    "The run has no ability to write files in the workspace."
  ],
  "budget": {
    "max_total_tokens": 500000,
    "max_wallclock_s": 2700,
    "max_usd": 15.0
  },
  "metadata": {
    "source": "AOS bridge exec-path preflight",
    "note": "Throwaway. Delete EXEC_PREFLIGHT.txt after it passes."
  }
}